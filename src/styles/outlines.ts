type BorderRadius = "base" | "max"
export const borderRadius: Record<BorderRadius, number> = {
  base: 10,
  max: 9999,
}

type BorderWidth = "thin" | "base"
export const borderWidth: Record<BorderWidth, number> = {
  thin: 1,
  base: 2,
}
